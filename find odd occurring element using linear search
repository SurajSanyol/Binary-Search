#include <iostream>
using namespace  std;
int main() {
  // std::cout << "Hello World!\n";
  int arr[]={1,1,2,2,3,3,4,4,3,5,5,600,600};
  int size =sizeof(arr)/sizeof(arr[0]);
  int ans = 0;
  for(int i=0;i<size;i++)
    {
      ans=ans^arr[i];
      
    }
  cout<<"printing the odd number of occurring  element in the array: "<<ans;
}


// this code is linear search ;
//TimeComplexCity (TC) = O(n);
